# ============================================
# Daily Mini Crossword - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ============================================
# Application Settings
# ============================================
APP_NAME=Daily Mini Crossword
DEBUG=false

# Secret key for session/CSRF (generate a random 32+ char string)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-min-32-chars

# ============================================
# Database
# ============================================
# Local development (SQLite):
DATABASE_URL=sqlite:///./crossword.db

# Production (PostgreSQL) - Render will provide this automatically:
# DATABASE_URL=postgresql://user:password@host:5432/database

# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT tokens (generate a random 32+ char string)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-jwt-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ============================================
# Server (for local development)
# ============================================
# Render provides PORT automatically, these are for local dev only
HOST=0.0.0.0
PORT=8000

# ============================================
# Render-specific (set automatically by Render)
# ============================================
# PORT - Render sets this automatically, do not set manually
# RENDER - Set to "true" when running on Render
